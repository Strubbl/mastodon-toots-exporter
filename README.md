# mastodon-toots-exporter

## Usage

```
go get -u gitlab.com/Strubbl/mastodon-toots-exporter
```

If you have no config yet, you can create a dummy config with the following command:
```
mastodon-toots-exporter -dummy
```
